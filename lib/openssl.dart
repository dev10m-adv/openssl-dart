/// OpenSSL **libcrypto** C API for Dart via FFI.
///
/// Bindings mirror the public libcrypto surface (no libssl/TLS). You manage
/// memory and object lifetimes as in C; see the official OpenSSL documentation.
library;

export 'src/third_party/openssl.g.dart';
