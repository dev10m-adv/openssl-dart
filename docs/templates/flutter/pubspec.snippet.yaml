# Copy into your Flutter app (see docs/FLUTTER.md).

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.5
  openssl:
    git:
      url: https://github.com/advforks/openssl-dart.git
      ref: v1.2.5   # pin tag or commit SHA

environment:
  sdk: ^3.12.0
  flutter: ">=3.38.0"
